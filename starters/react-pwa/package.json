{
  "name": "civitai-react-pwa-starter",
  "version": "0.0.0",
  "private": true,
  "description": "Vite + React 19 PWA starter (with Hono BFF) for building Civitai OAuth + Buzz-powered apps.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "civitai",
    "oauth",
    "pkce",
    "starter",
    "template",
    "vite",
    "react",
    "pwa",
    "hono",
    "bff",
    "ai",
    "generation"
  ],
  "homepage": "https://github.com/civitai/civitai-app-starters/tree/main/starters/react-pwa#readme",
  "bugs": "https://github.com/civitai/civitai-app-starters/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/civitai/civitai-app-starters.git",
    "directory": "starters/react-pwa"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsc -p tsconfig.server.json",
    "start": "node --env-file=.env dist-server/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.server.json --noEmit",
    "check:env": "node scripts/check-env.mjs",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:install": "playwright install --with-deps chromium",
    "probe:static-serving": "node scripts/probe-static-serving.mjs",
    "probe:cookie-flags": "node scripts/probe-cookie-flags.mjs"
  },
  "comment-overrides": [
    "SECURITY CONSTRAINTS ON TRANSITIVE DEPENDENCIES. Declared HERE, in the",
    "starter's own manifest, because that is what travels: `npx tiged",
    "civitai/civitai-app-starters/starters/react-pwa my-app` copies this",
    "directory alone, and the monorepo root's pnpm.overrides is NOT part of the",
    "copy -- so a constraint kept only there protects the monorepo and nothing a",
    "developer actually runs. Measured in starters/sveltekit-app before this",
    "existed: a scaffolded copy resolved cookie@0.6.0 and `npm audit` reported 3",
    "low advisories (GHSA-pxg6-pf52-xh8x) while the monorepo installed 0.7.x.",
    "NEITHER ENTRY HAS A REFERENT IN THIS STARTER TODAY: it resolves no `cookie`",
    "at all, and no `postcss` below 8.5.10 -- both are inert no-ops here. They",
    "are mirrored anyway so the rule stays mechanical: the alternative is a",
    "hand-maintained per-starter exception list that goes stale the first time a",
    "dependency tree shifts.",
    "BOTH blocks on purpose: `overrides` is npm's key, `pnpm.overrides` is",
    "pnpm's, and an external developer may reach for either. The `@<range>`",
    "SELECTOR spelling is used in both -- npm accepts it, and unlike the bare",
    "spelling it does not trip `EOVERRIDE` where the manifest also depends on",
    "the package directly (next-app devDepends on postcss).",
    "EXPECTED NOISE: a workspace `pnpm install` prints `The field",
    "\"pnpm.overrides\" was found in ... This will not take effect` for this file.",
    "That warning is correct -- inside the monorepo only the ROOT manifest's",
    "overrides apply -- and it is not a reason to delete this block: the block is",
    "for the tiged'd copy, where this manifest IS the root. Measured: pnpm does",
    "NOT read the plain `overrides` key (a scaffolded copy carrying only that key",
    "resolved cookie@0.6.0), so both blocks are load-bearing.",
    "Enforced by scripts/check-starter-workspace-overrides.mjs rule 4."
  ],
  "overrides": {
    "cookie@<0.7.0": "^0.7.0",
    "postcss@<8.5.10": ">=8.5.10"
  },
  "pnpm": {
    "overrides": {
      "cookie@<0.7.0": "^0.7.0",
      "postcss@<8.5.10": ">=8.5.10"
    }
  },
  "dependencies": {
    "@civitai/app-sdk": "^0.51.0",
    "@civitai/components-react": "^0.5.0",
    "@civitai/theme": "^0.4.0",
    "@hono/node-server": "^2.0.4",
    "@t3-oss/env-core": "^0.13.0",
    "hono": "^4.12.23",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@hono/vite-dev-server": "^0.26.0",
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.9.1",
    "@types/react": "^19.2.15",
    "@types/react-dom": "^19.0.0",
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-react": "^6.0.2",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.9.2",
    "vite": "^8.0.14",
    "vite-plugin-pwa": "^1.1.0"
  }
}
