/**
 * `civitai publish` releases a new block version after `civitai upload`.
 * Lands with v2 inline mode.
 */
export async function publishCommand(): Promise<void> {
  // eslint-disable-next-line no-console
  console.log(
    'civitai publish — coming soon.\n' +
      'Releases a new block version after asset upload.\n' +
      'Not applicable to v1 iframe blocks.',
  );
}
