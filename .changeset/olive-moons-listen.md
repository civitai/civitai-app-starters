---
'@civitai/app-sdk': patch
---

chore(sdk): add the `krea` image-gen engine to `IMAGE_GEN_ENGINES` and the transcribed spec fixture — the live orchestrator spec gained `KreaImageGenInput`, so the catalog drift-check was red on every PR
